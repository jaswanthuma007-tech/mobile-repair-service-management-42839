import React from 'react';
import '../ui/theme.css';

// PUBLIC_INTERFACE
export default function AdminPage() {
  /** Admin area placeholder (protected). */
  return (
    <div>
      <h1 style={{ marginTop: 0 }}>Admin</h1>
      <p style={{ color: 'var(--ocean-muted)' }}>
        Placeholder for admin operations, statistics, and payments management.
      </p>
    </div>
  );
}
